# RLM integration dependencies — pin to tested versions
# Install with: pip install -r requirements-rlm.txt

# Core
anthropicai>=0.32.0           # Claude root + sub model client
RestrictedPython>=7.0.0       # REPL sandbox
tenacity>=8.3.0               # Retry logic (used by adapters)
aiohttp>=3.9.0                # Async HTTP (transitive)

# LangGraph (must match existing DRS version)
langgraph>=0.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0

# Optional: sentence-transformers for OscillationDetector (existing DRS dep)
# sentence-transformers>=2.7.0
