# Additional test/runtime dependencies for QA gates and CI runners.
# Keep pinned and explicit so qa-p2 contract suite is reproducible.
httpx==0.27.0
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-xdist==3.6.1
