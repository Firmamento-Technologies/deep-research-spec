fastapi==0.111.0
uvicorn[standard]==0.30.0
redis[hiredis]==5.0.7
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.31
alembic==1.13.2
pydantic==2.8.0
pydantic-settings==2.3.4
python-multipart==0.0.9
httpx==0.27.0
minio==7.2.7
prometheus-client==0.20.0
python-dotenv==1.0.1

# ── Spaces / RAG ─────────────────────────────────────────────────────────────
# PDF → Markdown: rileva automaticamente PDF programmatici vs scansionati
pymupdf4llm>=0.0.17
# DOCX parsing (puro Python, nessuna dipendenza ML)
python-docx>=1.1.0
# OCR per PDF scansionati e immagini (richiede tesseract-ocr installato nel container)
pillow>=10.4.0
pytesseract>=0.3.10
# URL → Markdown: scraper senza API key, puro Python
trafilatura>=1.9.0
# Embedded vector store: file-based, Apache Arrow, sub-5ms query
lancedb>=0.10.0
# Embeddings locali: all-MiniLM-L6-v2 (~90MB), 384-dim, gratis
sentence-transformers>=3.2.0
